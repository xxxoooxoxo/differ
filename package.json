{
  "name": "differ",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --filter @differ/web dev",
    "dev:server": "bun --watch run packages/cli/src/index.ts",
    "build": "bun run --filter @differ/web build",
    "build:cli": "bun run build && mkdir -p dist/web && cp -r packages/web/dist dist/web/ && bun build packages/cli/src/index.ts --compile --outfile dist/differ && node -e \"console.log('\\n✅ Built standalone binary: dist/differ\\n\\nTo install globally, run:\\n\\nsudo ln -sf ' + process.cwd() + '/dist/differ /usr/local/bin/differ\\n')\"",
    "typecheck": "tsc -p packages/web/tsconfig.json --noEmit && tsc -p packages/server/tsconfig.json --noEmit",
    "differ": "bun run packages/cli/src/index.ts"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "@types/node": "^25.0.8",
    "bun-types": "^1.3.6",
    "shadcn": "^3.6.3",
    "typescript": "^5.9.3"
  }
}
